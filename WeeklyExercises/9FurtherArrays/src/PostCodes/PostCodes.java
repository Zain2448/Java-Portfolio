package PostCodes;

public class PostCodes
{
    /*
       3) Post Codes
            • Design and implement and application that reads a sequence of up to 25 pairs of names and postal codes
            for individuals.
            • Store the data in an object designed to store a first name, last name and postal code (all as strings).
            • Assume that each line of input will contain three strings, each separated by a tab character.
            • After the input has been read, print the list in an appropriate format to the screen.
     */
}
